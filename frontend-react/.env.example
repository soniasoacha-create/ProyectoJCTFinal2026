# Frontend - React
# En desarrollo local con proxy normalmente no se requiere.
# En despliegue (Vercel/Netlify), define esta variable con la URL publica del backend.
REACT_APP_API_BASE_URL=http://localhost:3000/api
